from django.apps import AppConfig

class InstructionsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.doc'  # Certifique-se de que este nome esteja correto
